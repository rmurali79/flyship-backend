# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/logistics_app?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=logistics_user
spring.datasource.password=7812
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=supersecretkey
app.jwt.expiration-ms=86400000

# Mail (Ethereal for dev)
spring.mail.host=smtp.ethereal.email
spring.mail.port=587
spring.mail.username=nwwahjgnp3kst72a@ethereal.email
spring.mail.password=aEPEuCqXfERHXuhQnE
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Stripe config import
spring.config.import=optional:classpath:stripe.properties

# Stripe keys (placeholders - actual values in stripe.properties)
#stripe.secret.key=
#stripe.publishable.key=

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${user.dir}/uploads
app.gcs.bucket=
